# Later nominal-regression witness

Diagnostic only. Protocol: issue35/comment5800483298, fixed before new outcomes.

Reconstruct seed41004's original4096-update prefix and the completed extra-fresh128-update continuation exactly. Reuse the existing collector, extra critic fitter, evaluator and live-state clone/optimizer replay. Verify all128 fresh corpora, extra optimizer logs and update records against artifact10752868007; compare all available checkpoint and detailed ordinary files.

Measure all three short panels on the same64 cases at each checkpoint4128..4224. Select the largest positive one-update nominal deterministic discounted-return drop, earliest exact tie. No positive drop means no manufactured witness. Selection is exposed; no production policy is selected.

Retain the selected actual incoming session, batch, pre-action simulator states and ordinary outgoing session. Reproduce its optimizer transaction and next identical-data transaction. The Adam observation hooks are reused byte-for-byte from Git blob e7bd92a5feb54ed1999a8a32d59a5d59f59e5630; the added preparer only inserts test-only calls and appends a child, leaving the original training statements unchanged.

At the same policy use8 paired1024-step continuations per fitting state/action, with distinct credit-key XOR0x2000000000000000. Keep the inherited true-terminal and far-critic-bootstrap semantics. One counterfactual changes only the actor baseline to the mean sampled V continuation estimate, rounded to float32, preserving original returns, actor Adam history, minibatch order and critic targets. Original captured advantages remain authoritative for the sham. No head fit or trajectory centering is introduced.

Independent confirmation uses512 paired cases per short panel, new domain0x74000000, for incoming/original/baseline-only actors. Retain discounted and undiscounted returns, completion counts and individual gains/losses. Do not reselect when independent behavior disagrees. Further gradient interpretation must use disjoint draw halves where independence is claimed; these sampled, bootstrapped estimates are not exact values.

Maximum new diagnostic transitions:38141952 localization,16777216 paired-credit,9437184 confirmation, plus the original-prefix/128-update replay and its1572864 extra data transitions. This is not sample-efficient online training or sustained-balancing qualification. No production defaults, master or deployed weights are changed.

Preflight includes Python evidence/attachment controls, native selector and captured-sham controls, strict Clippy, ordinary regression tests, formatting and protected-source restoration. Heavy execution is explicit and preserves failures. Source and all measurement artifacts are emitted by .github/workflows/ppo-late-nominal.yml. Until that run completes and its outputs are checked, no new behavioral result is claimed.
